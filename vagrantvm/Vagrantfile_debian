Vagrant.configure("2") do |config|
  config.vm.provider "docker" do |d|
    d.image = "debian"
    d.name = 'inputname-debian'
    d.remains_running = true
    d.cmd = ["/bin/bash", "-l"]
    d.ports = ["8080:inputport"]
    d.has_ssh = false
    d.cmd = ["ping",  "127.0.0.1"]
  end
end
